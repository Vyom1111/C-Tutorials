#include<stdio.h>
//sorting with the help of an array
int main() {
  int a[], temp, i, j, n;
  printf("Enter how many numbers you want to enter");
  scanf("%d", &n);

  for(int i=0; i<n; i++){
    printf("Enter the element %d", i +1);
    scanf("%d", &a[i]);
  }

  for(i = 0; i<n - 1; i++) {
    
  }
  return 0;
}